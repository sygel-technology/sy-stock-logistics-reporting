This module allows to automatically send picking reports to a printer. It
is possible to link users, printers a documents, so in case a given user
validates a stock picking, the used printer for certain reports can be
configured.
